const { Api, JsonRpc, RpcError, JsSignatureProvider } = require('eosjs');
const ecc=  require('eosjs-ecc');

export default {
    data(){
        return{
          
        }
    },
    methods:{
       
    }
}